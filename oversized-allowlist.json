{
  "_comment": [
    "Product source files permitted to exceed the 1000-line limit. POLICY-OWNED: the contents are set by",
    "the reviewer, not inferred from whatever happens to be oversized today. Adding a path here is a",
    "decision; removing one is just bookkeeping after a file is cleared.",
    "",
    "Approved by comms-senior-dev on 2026-08-13, verbatim: 'Allowlist should be explicit and policy-owned,",
    "not inferred from whatever is currently oversized. The currently justified product allowlist is the",
    "five decision/ceiling files.'",
    "",
    "ONE SOURCE OF TRUTH, read by both gates -- service/tests/test_no_new_oversized_source_file.py and",
    "mcp/stdio/tests/no-new-oversized-source-file.test.js. Two copies of a list like this is the forked-",
    "constant class the whole v0.5.x series has been removing; a duplicated allowlist would drift silently",
    "and each gate would then enforce a different policy.",
    "",
    "PATHS, NOT BASENAMES. The first version of these gates matched on basename, which would have exempted",
    "any file called app.js or server.js anywhere in the tree. Repo-relative, forward slashes.",
    "",
    "Both gates fail if a listed path no longer exists or has dropped below the limit, so this list shrinks",
    "honestly and cannot rot into names nobody re-checked."
  ],
  "limit": 1000,
  "allowed": [
    {
      "path": "mcp/stdio/server.js",
      "reason": "MEASURED 2026-08-14 (docs/APP_JS_STATE_MODULE_PACKET.md, 'the three remaining files'): 7 of 47 top-level functions, 88 lines, reference nothing at module scope and are movable; the file reaches ~2,918. 1,199 lines sit outside any function and 585 are comments. An earlier figure of 532 lines was produced by a checker that counted only CALL targets and missed variable reads - do not use it. Clearing this file needs a redesign, not relocation; it is also the live MCP bridge, which is why the standing ruling puts it last."
    },
    {
      "path": "service/new_dashboard/app.js",
      "reason": "MEASURED 2026-08-14 (docs/APP_JS_STATE_MODULE_PACKET.md): only 6 functions, 28 lines, reference nothing at module scope - the file reaches ~4,876. A prototype proved that moving the `state` object frees ~202 lines, NOT the 2,651 first claimed: 122 of 164 functions call a sibling defined here and 141 sit in ONE connected component of 3,085 lines. Clearing it needs that component broken - a redesign of the render flow."
    },
    {
      "path": "mcp/stdio/hermes-managed-host.js",
      "reason": "CORRECTED 2026-08-14 by GROUP measurement (scripts/js_free_functions.py, group mode). The earlier per-function figure - 3 functions / 67 lines, \"needs a redesign\" - was wrong in the way that matters: the per-function view counts every intra-group call as a blocker, so a cluster that moves cleanly as a UNIT reads as immovable. Measured as a group, runDeliveryLoop + deliverRun + runPollCycle + noTuiAttachedMessage + classifyClaimError = 998 lines and the file drops to 691; all 8 tuning constants are read nowhere else. NO REDESIGN NEEDED - it is a two-module relocation (998 in one module would be a fresh violation), split on the boundary between what drives the iterations and what one iteration does. Built and verified in this session (bodies byte-identical, 125/125 host tests) but NOT landed: tests/hermes-gateway-extraction.test.js reconstructs this file byte-for-byte from every extracted module, so the EXTRACTIONS plan must be appended in the same change. WIP and four specific traps are in project memory v05x-structural-refactor-series."
    }
  ]
}
