# search

Search inbox messages and shared artifacts using comms_search.

## Arguments
- `$ARGUMENTS` — Search query

## Instructions
Call comms_search with the query and `agentId` = your registered agent ID (without it, messages are
not searched, only shared files). Search scope "all" by default.
