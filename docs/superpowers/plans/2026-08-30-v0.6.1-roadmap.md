# v0.6.1 roadmap — the operator's scope, 2026-08-30

> Durable state for a long multi-compaction run. Update the STATUS column in the same
> change as the work, never afterwards. A row that says DONE without a sha is a claim,
> not a record.

## The rule that governs every row

**The fleet is LIVE and the operator is running teams on it.** Build, test and commit
freely. Do NOT rebuild the container, restart the environment bridge, or re-run
`install.sh` without the operator's go-ahead — the cutover happens when teams are idle.
Never run a bare `aify-comms`.

Everything new gets dev review before it is called done.

## Ordering

The operator said "address these in row". Row order is the order below. A row blocked on
review does not block the next row's INVESTIGATION, only its landing.

| # | Row | Status | Evidence |
|---|-----|--------|----------|
| 0 | Dev review of the v0.6 cutover, fixes land in v0.6 | DISPATCHED run_1788111413666_7d9efde3 | — |
| 1 | Dashboard browser pseudo-terminal: fast and good. Hermes' dashboard is the reference — they have worked on theirs a long time | NOT STARTED | — |
| 2 | Dashboard design / UI / UX pass: find issues, fix them | NOT STARTED | — |
| 3 | Tool triage: are all tools reasonable, do they have good descriptions, right params for real use cases | NOT STARTED | — |
| 4 | Security: were the v0.6 security changes actually DEVELOPED and TESTED? Finish whatever is not | NOT STARTED | — |
| 5 | aify-env + aify-wrapper真 SoC, judged by one question: can the operator start a NEW project on them with no aify-comms connection? | NOT STARTED | — |
| 6 | aify-env TUI/TUX upgrade. Currently good but minimal. Operator's idea: a right-side per-terminal view with an input option. More colours and indicators. Useful, not decorative | NOT STARTED | — |
| 7 | Claude Code's new cross-session agent communication (shipped by Anthropic, similar problem to aify-comms). Study it; harvest anything worth adopting — guides, texts, skills, protocol ideas | NOT STARTED | — |

## Version policy, as instructed

- **v0.6 = current.** Fixes only. The tag moves once the dev review closes and its fixes land.
- **v0.6.1 = everything above.** New work, improvements.

## What "done" means for this run

All seven rows tested, fixed, documented, dev-reviewed, polished and pushed. Then the
loop is removed. Not before.
