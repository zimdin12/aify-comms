# aify-comms v0.2 — release plan

**Status:** proposed. Written 2026-07-26, after v0.1 shipped at `59db55b`.

**N9/N10/N11/N13 FIXED AND DEPLOYED 2026-07-31** — the "Online envs" card now derives instead of
counting registrations; "Continue in CLI" names the machine the session lives on and picks
`CODEX_HOME` by session mode; and `aify-doctor`'s `bridge-installed` no longer reports the bridge
stale for commits that never touched it. Container rebuilt, all three clients reinstalled,
`aify-doctor` reports all checks passing.

**PARTLY SHIPPED as v0.1.1 (2026-07-29).** Deployed and tagged, so this file is no longer
all-future. Done since it was written:
- **N7** (degraded-environment stop) — the hotfix this file recommended taking ahead of the release.
- **C8's honesty half + the real input-path defects behind it** — the dashboard wheel handler was
  bypassing the serialized input queue AND firing on hover, injecting synthetic arrows into whichever
  agent's PTY the pointer was over. That, not the console-input endpoint, was corrupting drafts.
- **C9's actionable half** — mouse traffic no longer reaches the PTY from a wheel gesture.
- **Wrapper symmetry** — `codex-aify` now recovers its agent id from a bare `--resume`, the gap
  `hermes-aify`'s own comment had admitted; plus a `comms_register` warning for identity-less
  residents, which is the runtime half of the same problem.
- A round of operator-reported chat/session UX defects (sticky queue mode, subject echo, composer
  focus, one-agent-one-session-row).

Still open: **WS-1** (the `api_v2.py` carve and its duplicate-rule guard), **WS-2** (the console
submit MECHANISM — only the honesty half and the input-path defects shipped), **WS-3** (Windows-real
deploy verification / bridge build stamp on heartbeat), **WS-4** (the hermes deltas), **WS-9**
(`aify-comms` as a live status screen), **WS-5/6/7**, and **WS-8** (the compaction dwell, still last
and still gated). **WS-10 is withdrawn**, not open.

This is the *plan*. The *findings ledger* is [V0.2_PLAN.md](V0.2_PLAN.md) — every item there was
traced to code and several were corrected or withdrawn under review, so it is evidence, not a wish
list. This file decides what v0.2 actually is, in what order, and what gates each step. Items are
referenced by their ledger id (A1b, B1, C6 …) rather than restated.

---

## What v0.2 is for

v0.1 was a **stability** release: a safe revert point, service-breaking defects only. It took
**five review rounds**, and the shape of those rounds is the argument for this release:

| Observation from the v0.1 series | What it says |
|---|---|
| Every one of the 14 defects passed a green suite (958–986 tests) | Green is not evidence in this repo |
| Two defects were *inside fixes I had just written and verified* | The risk is in the contract around a change, not the change |
| One rule (`terminal is not active`) was implemented **twice**, and the first fix landed in only one copy — changing nothing | Duplicate rules are undetectable by review at this file size |
| `api_v2.py` is **22,889 lines** — 44× the repo's own 500-line guidance | Nobody, human or agent, holds it in context |

So v0.2 is a **structural and observability** release. Not a feature release, and deliberately not
"the v0.1 nits." Four things earn the version bump:

1. **Close the honesty gaps that cost real operator time** — the console submit that reports success
   on input the TUI never acts on (WS-2), and the fact that on the primary dev host *nothing* verifies
   a running bridge is the installed bridge (WS-3).
2. **Give the operator somewhere to look.** Every incident this month was diagnosed by hand-writing
   SQL against the container, and `aify-comms` — the process that knows most — prints a rolling log
   that loses the one line that mattered. WS-9 fixes that. (An earlier draft paired this with
   container observability; WS-10 is withdrawn — that subsystem has zero defined containers, so
   making it observable would have been effort spent on code nobody runs.)
3. **Pay down the file that caused the defect run** (WS-1). Every recurring defect class here — a
   rule implemented twice, a named set reused for the wrong purpose, a picker that shadows a live row
   with a dead one — is a symptom of a module too large to reason about. Three of this round's own
   findings (N1, N2, N3) are that class.
4. **Port what an upstream with the same problem already solved** (WS-4), from a direct read of
   hermes' dashboard-embedded terminal.

See "Release sequencing" for what actually ships in v0.2.0 — WS-1's carve is deliberately *not* the
gate, and the reasoning for that changed after the first draft.

---

## Ground rules (carried from v0.1, non-negotiable)

- **Never state a deployed build sha in a commit, tag or doc.** `service/_build_stamp.json` is
  gitignored and untracked, so `GET /version`'s sha is host-local state. Three review rounds were
  burned arguing `f291161` vs `5885eef`; both were right about their own machine.
- **Single-worker uvicorn.** `_LIVE_STATE_CACHE` and `_LIVE_SCREENS` are process-global. WS-1 must
  not change that, and must keep cache ownership in exactly one module.
- `bash scripts/stamp.sh` **before** `docker compose up -d --build`.
- Bridge changes need `install.sh` **and** a wrapper relaunch. On Windows nothing proves the
  relaunch worked — that is WS-3.
- Skill mirrors `.claude/skills/` and `.agents/skills/` stay byte-identical.
- **The suite must be green on Windows**, not only on Linux (E2). The installer-contract test was
  red on the primary dev host for the entire v0.1 series.

### Scope discipline (added 2026-07-31, after this plan violated it)

- **Before improving a subsystem, prove something uses it.** Measured 2026-07-31 on the live
  service: `GET /api/v1/containers` returns **503 "Container manager not initialized"**, and
  `Container manager` appears **zero times** in the service log — because `main.py:382` only
  constructs the manager `if config_data.get("containers", {}).get("definitions")`. So it is not
  "zero containers defined"; the subsystem is **never instantiated at all**. Three findings in this file (N4/N5/N6) diagnose 740
  lines of Docker orchestration that nothing runs, and WS-10 proposed investing further in it. That
  is the opposite of the judgement applied everywhere else here. Ask "should this exist?" before
  "how do we make it observable?".
- **Priority = load-bearing × invisible, NOT visible × recently-reported.** Operator-reported
  dashboard defects are real and get fixed, but they arrive with an advocate. `api_v2.py` at 22.9k
  lines and the Windows deploy-verification gap have none, are where the recurring defect classes
  actually come from, and therefore keep losing the day. A plan that only tracks what was reported
  will always under-serve them.
- **Prefer the cut that removes code.** The cheapest correct outcome in this repo has repeatedly
  been a deletion (retention sweep, the reminder revert, the sticky queue checkbox). Reach for that
  before designing an addition.

---

## New findings from this round

Twelve. N1–N3 are session/console state, N4–N6 are containers, **N7 was a live defect found by
review — FIXED and shipped in v0.1.1**, **N8 is a live defect found by a teammate on 2026-07-31**,
and **N9–N12 come from the 2026-07-31 bug-hunt round** (below). Two of those four are in code *I
shipped in v0.1.1*, which is the third time this project has caught a defect inside a fix that had
just been reviewed and declared done.

Findings are numbered in discovery order, not document order: N7/N8 sit early because they are
FIXED and NEW respectively, and N9-N12 are grouped at the end of the findings section.

Two are bugs I did **not** file — N2, and the container restart-loop at the end of N6 — because the
code contradicted a plausible reading. And **N4 is a finding a reviewer corrected**: I asserted it
from a truncated read of the very function I was citing. Both directions are recorded rather than
tidied away, because the failure mode of this document is confident prose over unverified claims.

### N1. The session-status vocabulary is aspirational, not actual — and it has already caused two defects

Measured against the live DB (read-only, 149 `agent_sessions` rows of real history):

```
agent_sessions.status    → ended 82 | stopped 51 | running 10 | lost 4 | failed 2
agent_sessions.terminal_status → '' 134 | attached 9 | stopped 6
```

**Five values.** But the file defines three overlapping sets naming eleven:

- `_LIVE_SESSION_STATUSES = {starting, running, recovering, restarting, cli-takeover}` (`api_v2.py:379`)
- `LIVE_SESSION_STATUSES = {running, attached, active, idle, starting, recovering}` (`:394`), whose
  docstring claims it is "the FULL set of `agent_sessions.status` values that count as live"
- `ENDED_AGENT_SESSION_STATUSES` (added in v0.1 as the documented complement of the above)

`attached`, `active` and `idle` **never appear in that column** — `attached` is written to
`terminal_status` (`:3472`), a different column. So `LIVE_SESSION_STATUSES`' docstring is wrong
about its own subject, and every `status IN (...)` list built from it carries dead branches.

This is not cosmetic. It has produced two shipped defects already:
- **slip 6:** `_SESSION_DELETE_ALLOWED_STATUSES` (a deletion allowlist) reused as a hide-from-list
  set, breaking `comms_restart` / `comms_compact` / Restart / Reset / Compact;
- **R2c:** `_current_agent_session_row` promoting only four statuses in its `ORDER BY`, so a live
  session lost the `last_seen` tiebreak to a fresher corpse.

Both are the same failure: reasoning about a vocabulary nobody had measured.

- [ ] Pin the ACTUAL vocabulary with a test that asserts the set of values any writer can produce,
      derived from the writers, not from the constants.
- [ ] Delete phantom members, or — if a writer is *supposed* to produce them and doesn't — file that
      as the real bug. Decide by reading the writers, one at a time.
- [ ] Only then simplify the three sets. Not before: a set's name encodes its purpose and collapsing
      them is exactly the move that broke `comms_restart`.

### N2. `assign_agent_environment` marks the wrong set 'lost' — LATENT, not live. Filed as a trap, not a bug.

`POST /agents/{agent_id}/environment` (`api_v2.py:14286`) rewrites `environment_id` for **every**
session row of the agent (`WHERE agent_id = ?`, no id filter), but only marks 'lost' those whose
status is in `{starting, running, recovering, restarting}` (`:14415-14416`) — the narrow
`_LIVE_SESSION_STATUSES` minus `cli-takeover`, not `LIVE_SESSION_STATUSES`.

I was about to file this as a live defect: a session with status `attached`/`active`/`idle` would
survive with a live status while its `environment_id` now points at an environment it never ran on,
and `_current_agent_session_row` accepts exactly those statuses as current — so the picker would
answer with a session claiming to live on a bridge that does not own its PTY.

**N1's measurement says that cannot currently happen.** The only live value the column takes is
`running`, which *is* in the narrow set, so live rows do get marked lost. The defect is unreachable
today and the code is correct in practice.

It stays on the list as a **trap**: the moment any writer starts producing `attached`/`active`/`idle`
in that column — which `LIVE_SESSION_STATUSES` actively invites — this UPDATE silently stops marking
them lost, and the failure is a session pointing at the wrong bridge. Recording it so the next
reader does not have to re-derive it, and so N1 has a concrete consequence attached.

- [ ] No code change until N1 settles the vocabulary. Then either widen this set or delete the
      phantom statuses that make it wrong.
- [ ] Note the adjacent `SELECT id FROM agent_sessions WHERE agent_id = ? ORDER BY last_seen DESC
      LIMIT 1` a few lines below (`:14423`) — newest-by-`last_seen`, **no status filter**. Same
      shadowing shape as R2c. Harmless only because of what N1 measured.

### N7. **FIXED** (hotfix) — a `stop` in a DEGRADED environment was still cancelled, permitting a duplicate worker

Reviewer finding, independently verified, **now fixed** — see DECISIONS.md,
*"This environment's bridge can act" has ONE definition*. This was the same worker-duplication chain
v0.1 claimed to fix **at the root**, still open for degraded environments — the third consecutive
round in which my fix covered the narrow case.

**Reachability, measured before fixing** (the discipline the last three rounds cost me): the live
fleet's `environments.status` holds only `{online, offline, forgotten}`, and **no bridge reports
`degraded`** — it arrives only from an explicit registration or a status write, and nothing derives
it. So this was latent-but-reachable rather than firing today, which is why it shipped as a normal
reviewed hotfix rather than an emergency. It was fixed anyway: eleven gates accept `degraded` and the
sweeps did not, and a disagreement that wide gets reached eventually and silently.

**Two divergences closed, not one.** Raw `status = 'online'` also never *ages*, so a silent ONLINE
bridge kept its controls pending indefinitely while `api_v2` already treated that environment as
offline. The tests pin both — and the stale-online subtest failed before the fix, which is how that
second divergence was found rather than assumed.

**Scope held deliberately.** While verifying I found `api_v2.py:15436` — an environment-inference
fallback whose `ORDER BY CASE WHEN status = 'online' THEN 0 ELSE 1 END` puts `degraded` in the same
tier as `offline`, so a fresher offline env outranks a degraded one on the `last_seen` tiebreak. Same
tiered-ORDER-BY shape as R2c and N2. **Not touched:** it is an inference heuristic I have not traced,
its blast radius is which `environmentId` gets guessed when nothing else is known, and bundling an
untraced behaviour change into a hotfix is how the last three rounds went wrong.
- [ ] Trace `api_v2.py:15436`'s five-ish callers, then decide whether `degraded` belongs in tier 0.

The asymmetry, both halves confirmed in code:

- **Reachability accepts degraded.** Eleven gates in `api_v2.py` test
  `env_status in {"online", "degraded"}` (`:1032`, `:2159`, `:3033`, `:4182`, `:5046`, `:5063`,
  `:5121`, `:7604`, `:7764`, `:8709`), and `_ENVIRONMENT_HEARTBEAT_STATUSES = {"online","degraded"}`
  (`:4075`). So a degraded environment keeps heartbeating and its bridge stays **live and working**.
- **Both reconcile sweeps require `online`.** In `db.py` (the stop re-target and the env-currency
  failure sweep, ~`:782-823`) every `environments` subquery carries `AND environments.status = 'online'`.

So for a degraded environment: the re-target finds no online row and **cannot rescue** the stop, and
the failure sweep's `NOT EXISTS (… status='online')` is satisfied and **fails** it. The full chain:

```
Stop worker → terminal 'stopping', stop queued
  → env is degraded (still heartbeating, bridge still working)
  → re-target finds no ONLINE row, does not fire
  → failure sweep fails the stop      ← the PTY is never killed
  → session written 'ended'
  → Start is permitted → SECOND worker, old PTY alive
```

Identical to the chain fixed in v0.1, reached through `degraded` instead of through a changed
`bridge_id`. And it is worse than a plain strand: the bridge in a degraded environment is alive and
*could* have executed the stop.

**What shipped** (all done in v0.1.1 — recorded as `[x]` so nobody re-implements a shipped fix):
- [x] The sweeps now agree with the reachability gates — `{online, degraded}` wherever the question is
      "can a bridge act on this?". The eleven gates were deliberately NOT narrowed: that would make
      degraded environments undeliverable, and a too-strict gate is what made the whole `ef-` team
      unstartable in v0.1.
- [x] Fixed in **both** implementations, via one shared helper, at all four call sites.
- [x] Tested per environment status across the whole `ENV_KNOWN_STATES` domain, including that a stop
      in an `offline` environment still fails (the accumulation bound survives).

### N8. **NEW** — the cold-start refusal names a cause it never checked

Reported live 2026-07-31 by graph-tech-lead after double-restarting ef-manager: the bridge said
*"no online environment can host managed claude-code for this agent"*, while the Windows environment
was online and advertising claude-code. They checked, concluded the message was wrong, and were
right.

**Verified against the live DB:** `windows:StevenZ-L:default` is `online`, `last_seen` fresh, and its
`runtimes` array contains `{"runtime": "claude-code", "modes": ["managed-warm"], "available": true,
"unavailableReason": ""}`. The message's claim was false.

**Mechanism.** `api_v2.py:16267` calls `_coldstart_spawn_request_for_dispatch(...)` and renders ONE
message for a bare `False`:

```python
coldstarted = await _coldstart_spawn_request_for_dispatch(...)
if not coldstarted and not await _has_claimable_spawn_request(db, recipient_id):
    not_started.append(... f"No online environment can host managed {runtime} for this agent ...")
```

But that function returns `False` from at least FOUR distinct causes, and its own docstring says so:
- the runtime is not in `{claude-code, codex, hermes, opencode, pi}`;
- **the agent is `session_mode='resident'`** — a deliberate resident-safety refusal, added 2026-07-06
  precisely so an auto-cold-start cannot fork a managed twin beside a resident;
- a claimable spawn request already exists;
- no environment can actually be resolved — the ONLY case the message describes.

So the operator is told to go fix their environments no matter which of the four fired. In this
incident the environments were healthy; the refusal came from transitional session state during the
double restart. The message sent a competent agent to investigate the one thing that was fine.

This is the same class as the v0.1 fixes and as C8: **a report asserting a cause it did not verify.**
It is cheap to fix and it directly cost an agent time today.

- [ ] Return a REASON (small enum), not a bare bool, from `_coldstart_spawn_request_for_dispatch`.
- [ ] Render the actual reason: "this agent is resident — switch it to managed first", "a spawn is
      already in flight", "runtime X is not cold-startable", "no online environment advertises X".
- [ ] Test each branch produces its own message. The bug is that four causes share one sentence, so
      a test that only covers the environment case would not have caught it.
- [ ] Check the sibling call site (`api_v2.py:16401`) — same sentence, same conflation.

### N3. `_resolve_live_console_terminal`'s two branches disagree about `stopping`

`api_v2.py:13931`. The pointer branch accepts any terminal whose status is not in
`_TERMINAL_END_STATUSES` (a complement), so it accepts `'stopping'`. The fallback branch uses an
explicit allowlist `('starting','attached','running','active','idle','recovering')`, which excludes
it. So `comms_console_input` / `comms_console_tail` can resolve onto a terminal that is being killed,
but only via the pointer.

Low blast radius, and it matters more after v0.1 than before: `'stopping'` rows now legitimately
persist while a queued stop is outstanding (R2a), so this is a live window rather than a
theoretical one. Writing input into a PTY being torn down is never right.

- [ ] Make both branches use the same allowlist. One-line change; needs a test that the pointer
      branch declines a `'stopping'` terminal, because the SQL alone will not express it.

> **N4–N6 diagnose a DORMANT subsystem.** Measured 2026-07-31: zero containers are defined on the
> live deployment, so none of this is reachable today. They are kept as an accurate description of
> the code, NOT as work items — see WS-10, which is withdrawn. If `service/containers/` is deleted,
> delete these with it.

### N4. Container state is partly discarded at the API boundary — **CORRECTED, was overstated**

**The first version of this entry was factually wrong and a reviewer caught it.** I claimed
`list_containers()` returns *none* of the diagnostic fields, including `error_message`. It does
surface it: `if state.error_message: entry["error"] = state.error_message` (`manager.py:353-354`).
I read `list_containers` from `:331` to the `idle_seconds` line, took that for the end of the dict
build, and asserted from the truncation. It was three lines further down. Same slip as concluding
from one table — **partial evidence read as complete evidence** — and it is why the entry is left
here corrected rather than quietly edited.

What is actually true, and it is narrower but still real:

- **The health-monitor path produces a reasonless `FAILED`.** `_health_loop` sets
  `state.status = ContainerStatus.FAILED` (`:443`) **without** setting `error_message`. Every *start*
  failure path does set it (`:228` health-check timeout, `:238` image-not-found with the exact
  `docker pull`, `:248` start-cancelled, `:261` generic), so the gap is specific to a container that
  was healthy and then failed its checks — which is the common runtime case rather than the boot case.
- **`consecutive_health_failures`, `started_at`, `last_request_at` and `container_id` are still not
  exposed.** So a container failing **2 of 3** health checks remains byte-for-byte identical, to every
  reader, to a healthy one. That half of the finding stands and is the substance of N5.

- [ ] Set `error_message` on the health-monitor transition, so a `FAILED` container always carries a
      reason no matter which path failed it.
- [ ] Expose `consecutive_health_failures`, `started_at`, `last_request_at`, `container_id`. No new
      state and no new computation — stop discarding what is already known.

### N5. There is no DEGRADED container state, so the signal is binary and late

The health loop keeps a container at `RUNNING` until `consecutive_health_failures >= retries`, then
jumps straight to `FAILED` and restarts (`manager.py:441-446`). `ContainerStatus` has six values and
none of them means "up but failing checks". So the observable story is *fine → dead*, with the
interesting interval invisible. This is the operator's "they just roll some heartbeat", precisely.

- [ ] Add a derived DEGRADED reading (`RUNNING` **and** `consecutive_health_failures > 0`). Derive
      it — do not add a seventh stored status. The status column is written from several paths and a
      new writable value is how the session-status vocabulary got into the N1 mess.
- [ ] Keep a small in-memory ring buffer of container transitions. A crash-restart cycle currently
      leaves **no trace at all** once the counter resets on the next good check, so "restarted 3× in
      the last hour" — the single most useful container fact — is unavailable everywhere. Process-global
      and single-worker, consistent with `_LIVE_STATE_CACHE`.

### N6. Per-container `health_check.interval_seconds` is configured and then ignored

`_health_loop` sleeps a hardcoded `await asyncio.sleep(10)` (`manager.py:420`). The configured
`interval_seconds` is honoured **only** during startup, by `_wait_for_health` (`:218`). So a
definition that sets a 60s interval is checked every 10s, and one that wants 5s cannot get it.

- [ ] Honour the per-container interval, or delete the field. A knob that silently does nothing is
      worse than no knob.

**Checked and NOT a defect** (recording it so nobody re-derives it): I expected a 10s restart loop —
the health loop sets `FAILED` and calls `restart_container` without resetting
`consecutive_health_failures`, so if the restart returned early the next tick would restart again
immediately, forever. It cannot: `restart_container` → `start_container` blocks on `_wait_for_health`
up to `startup_timeout_seconds` and sets the counter to `0` on success (`:225`), and raises on
failure. Slow-booting containers (vLLM, LocalAI) are safe.

### N9. The dashboard's "Online envs" card counts REGISTRATIONS and calls them reachable

`api_v2.py:22177`:

```python
env_c = await db.execute("SELECT COUNT(*) FROM environments WHERE status = 'online'")
```

Raw stored status, no staleness term. The card it feeds (`analytics.js:168`) is labelled
**"Online envs"** with the tooltip **"Environment bridges reachable right now (managed agents can be
spawned on these)"** — an explicit reachability claim, answered with a registration count.

**Why it is wrong rather than merely approximate:** nothing ever demotes a stale environment to
`offline`. Grepping every `UPDATE environments` writer (`api_v2.py:10286`, `:10449`, `:10619`,
`:10653`, `:11001`) finds registration, explicit disable, and a `last_seen` bump — **no
staleness-driven demotion anywhere**, and none in `db.py`'s reconcile either. Measured on the live
fleet: `wsl:StevenZ-L:default` reads `offline` at 56 days idle only because it shut down *cleanly*.
A bridge that crashes, is killed, or loses power keeps `status='online'` **forever**.

The codebase already knows the right answer: `_environment_effective_status(row, offline_seconds=…)`
exists precisely for this and is used at **thirteen** call sites in the same file. This card is one of the few readers
that bypasses it — and every sibling card in the same grid is derived (`_compute_agent_status`,
`_agent_wake_mode`). This one raw count sits among three honest ones.

**This is the exact false green `aify-doctor`'s `env-bridge` check was created to eliminate** — it
counted *registered* rows and reported "2 connected" with zero bridges alive, fixed in `756f3a5`.
CLAUDE.md documents that fix as the worked example of the class. The dashboard was never given the
same treatment, so the same lie survives in the surface the operator actually watches.

Not firing today (one env, heartbeat 24 s old) — reachable the moment a bridge dies uncleanly, which
is the common incident case and exactly when this number gets read.

- [ ] Count via `_environment_effective_status(...) in {"online","degraded"}`, not raw status. Same
      helper, same cutoff resolution as N7's fix, no new concept.
- [ ] Test with a stale-but-`online` row — a test using only fresh rows passes either way, which is
      how the doctor version survived so long.
- [ ] Audit the whole stats block for other raw-status counters while in there. Do not fix only the
      one named here: fixing only the reported value is the slip of the last three rounds.

### N10. "Continue in CLI" hands the operator a command for the wrong machine — **my v0.1.1 defect**

`cli-resume.mjs`. I added this in v0.1.1 because the drawer rendered **nothing** when there was no
resumable session, and "an absent block is indistinguishable from a broken feature". The file's own
header states the rule: *"a command that does not work is worse than an honest 'not supported'."*

It then builds `claude-aify --resume <handle>` / `hermes-aify --resume <handle>` /
`codex … resume <handle>` from the agent's runtime and handle — and **never checks which machine the
session lives on**. The command is copy-pasteable, unqualified, and implicitly claims "run this here".

**Measured on the live fleet:** 28 agents render a command. **8 of them are for a session on another
machine or another filesystem namespace** — `linux:laputa` (a different physical host),
`linux:stevenz-l`, `wsl-ubuntu:*` (same box, different `$HOME`, so a different rollout/session store):

```
graph-senior-dev  codex        wsl-ubuntu:stevenz-l     mp-manager    claude-code  wsl-ubuntu:laputa
tech-lead         codex        linux:stevenz-l          mp-senior-dev hermes       linux:laputa
Hermes-test       hermes       linux:laputa             lc-tech-lead  codex        linux:laputa
ci-senior-dev     hermes       linux:laputa             lc-coder      codex        linux:laputa
```

Verified end-to-end for the codex four: **none of their handles has a rollout file in either CODEX
home on this host** (19 rollouts in `~/.codex`, 0 matching). So the command cannot work, and nothing
on screen says so.

I replaced a silent gap with a confident falsehood. That is a strictly worse failure — the empty
block at least prompted the operator to ask, which is how the original report reached me. It is the
same class as N8 and C8 (**a surface asserting something it never verified**), introduced in the very
change that cited that class as its motivation.

- [ ] Compare the agent's `machine_id` against the machine the operator is on. When they differ,
      render the reason, not the command: "this session lives on `linux:laputa` — run it there."
- [ ] Where the command IS valid, say on which machine, so a copied command stays true when pasted
      into a doc or a chat.
- [ ] Test the cross-machine case explicitly. The existing tests all use same-machine fixtures, which
      is why this shipped green.

### N11. The codex resume command hardcodes the MANAGED `CODEX_HOME` for resident agents

Same file, the codex branch:

```js
CODEX_HOME="$HOME/.local/state/aify-comms/managed-codex-home" codex … resume <handle>
```

That path is the **managed** home (`runtimes-codex.js:256`, applied via `terminal-env.js:51` and
`codex-legacy-controller.js:223`). A **resident** codex session uses the default —
`install.sh:2869` and five siblings: `codex_home="${CODEX_HOME:-$HOME/.codex}"`.

**Measured: 3 of the 4 codex agents holding a session handle are `resident`** (`tech-lead`,
`lc-tech-lead`, `lc-coder`), so for three quarters of them the command points `codex resume` at a
store that cannot contain their rollout. On this host the managed home holds **0 rollouts** and
`~/.codex` holds 19 — the two are genuinely disjoint. This is the standing `no_rollout` symptom
(task #136) reachable through a second, self-inflicted route.

- [ ] Choose the home from `session_mode`: managed → managed home; resident → omit the override and
      let the wrapper's `${CODEX_HOME:-$HOME/.codex}` apply.
- [ ] Test both modes. This is one more instance of enumerating a domain: I wrote one branch for a
      two-valued field, which is the same shape as the `queueIfBusy`/`turn_busy`/`degraded` misses.

### N12. "Is this terminal active?" is implemented at least EIGHT times, in TWO different arities

This is the concrete target WS-1's duplicate-rule guard has been missing, and the v0.1 defect
(*one rule implemented twice, fixed in one copy, changing nothing*) is **still present today**:

| Where | Members |
|---|---|
| `_TERMINAL_ACTIVE_STATUSES` (`api_v2.py:330`) | starting, attached, running, active, idle |
| `db.py:809` — failure sweep | starting, attached, running, active, idle |
| `api_v2.py:5580` — `_reconcile_ended_terminal_controls` | starting, attached, running, active, idle |
| `api_v2.py:2514` | starting, attached, running, active, idle |
| `api_v2.py:678`, `:3082`, `:3198`, `:4520`, `:4597`, `:17261` | …**+ `recovering`** |

**Both sweeps are live and they are each other's duplicate.** `main.py:238` runs
`api_v2._reconcile_ended_terminal_controls`; `db.py:984` runs `_reconcile_terminal_controls`. Same
allowlist, same `status IN ('pending','claimed')`, same `LOWER(action) != 'stop'` exemption, same
`'terminal is not active'` error string. `db.py:780` **documents the duplication in a comment** — and
a comment cannot fail CI, which is the entire argument for the guard.

The arity split is the sharper half: six copies treat `recovering` as active, four do not. So the
answer to "is this terminal active?" depends on which copy the caller happened to reach.

**Reachability — measured, and it is LATENT, not live.** `recovering` appears in 36 allowlist
positions and has its own repair function (`_repair_superseded_recovering_sessions`, `:6157`), yet
**zero** `terminal_sessions` rows hold it and no writer sets it directly. So today the divergence
cannot fire. It is filed as a trap of exactly N2's shape: the moment anything writes `recovering`,
the two sweeps will fail that terminal's pending controls with *"terminal is not active"* while six
other queries still treat it as live.

- [ ] WS-1's guard test pins **this predicate**, not just the error string — one definition, imported.
      A guard that only greps `'terminal is not active'` would pass while six arity-divergent copies
      of the predicate remained.
- [ ] Resolve the `recovering` question by reading the writers (N1's method), then make every copy
      agree. Do not "just add recovering everywhere" — that is the collapse-the-sets move that broke
      `comms_restart`.

### Checked in this round and NOT defects

Recorded so the next reader does not re-derive them — the same discipline as N2 and N6's restart-loop:

- **Lexical timestamp comparison in the analytics window is SAFE.** `run_where`/`spawn_where`
  (`api_v2.py:22139-22142`) compare TEXT columns with `>=` against `_iso_from_ms` output. Dumped
  every stored value in `dispatch_runs.requested_at`, `.finished_at` and `spawn_requests.updated_at`:
  **one format, 100% of rows**, `YYYY-MM-DDTHH:MM:SSZ`, fixed width, matching the generator exactly.
  Lexical ordering therefore equals chronological ordering. This was on the list as a suspected
  instance of the known SQL-lexical-timestamp class; it is not one.
- **`CLI_RESUME_RUNTIMES` is complete.** Measured `agents.runtime` = {hermes 18, claude-code 15,
  codex 6, pi 2}. The set covers three; `pi` is excluded deliberately and documented (no resident
  `pi-aify` wrapper is installed). No missing runtime.
- **`running` / `starting` / `active` are NOT phantom terminal statuses.** A live snapshot showed only
  {stopped 69, attached 7, failed 6} and I was about to file four phantom members — then read the
  writers, which do produce `running`, `starting` and `active`. They are transient states a
  point-in-time snapshot misses. **A snapshot can prove a value EXISTS; it cannot prove one does
  not.** N1's measurement was of `agent_sessions`, where the conclusion still holds, but the method
  needs this caveat attached and it now has one.

---

## What the hermes study found

Read directly from the hermes 0.19.0 editable checkout on this host
(`~/AppData/Local/hermes/hermes-agent`), which embeds `hermes --tui` in its own dashboard over a
PTY — the same problem as our Session Console. Their path is
`web/src/pages/ChatPage.tsx` → `WebSocket /api/pty` → PTY → `ui-tui/dist/entry.js` + `tui_gateway`.

**Already ported** (the 2026-07-02 upstream study, task #237 — our code carries `hermes parity`
comments and matches): wedged-`CONNECTING` force-close on a budget, resume-signal reconnect throttled
across `visibilitychange`/`pageshow`/`focus`/`online`, capped exponential backoff, emit-resize-only-on-change,
double-rAF authoritative fit, Unicode11 + `allowProposedApi`, OSC 52 clipboard, `scrollback: 5000`.
Not re-litigating those.

**Worth taking** (verified deltas, four items — see WS-4):

1. **The burst-then-Return write discipline.** This is the big one and it lands on C8, not on the
   dashboard. `ChatPage.tsx:445-454` sends a slash command as text, waits **100 ms**, and *then*
   sends `\r` as its own frame. The comment states the reason: 100 ms is "safely past Node's default
   stdin coalescing window", so Ink's tokenizer "emit[s] a keypress event for each character (not
   coalesce them into a paste)". They do the same again for `/image` (`:587-591`) and for the
   `?learn=` seed (`:985`). It is a deliberate, load-bearing, thrice-repeated discipline.
2. **WebGL gated on width.** `const useWebgl = terminalTierWidthPx(host) >= 768` (`:772`), because
   the WebGL glyph atlas is sized in *device* pixels and renders visually much larger cells than
   `fontSize` implies on phones and in DevTools device mode. Ours loads WebGL unconditionally
   (`app.js:1888`).
3. **A close-code contract with UI for each outcome.** `4410` = the agent process exited → write
   `[session ended]` and render an explicit "Start new session" affordance; `4409` = superseded by a
   newer tab → stay quiet; `4401/4403/4404/4408` each get a specific banner, and *every* close logs
   `code` + server-supplied `reason` so a "chat won't connect" report is diagnosable without server
   access (`:1001-1069`). Our main dashboard socket's `onclose` (`app.js:535`) takes no argument at
   all; only the codex *synthetic* console prints a code (`app.js:2398`).
4. **Lifecycle predicates as pure, separately-tested modules.** `lib/pty-reconnect.ts` +
   `pty-reconnect.test.ts`, `lib/pty-mobile-input.ts` + test, `lib/chat-activation.ts` + test. Our
   equivalent logic is correct but inline in a 4,964-line `app.js`. This is precisely the structural
   fix that made doctor's false-green testable in v0.1 (B2) — and that extraction immediately caught
   a real bug (the `filter` arity trap) the moment it became testable.

**Deliberately NOT porting:**
- *The localStorage attach-token reattach* (`:67-88`). Our terminal identity is already server-side
  (`terminal_sessions` rows + `runtime_state.consoleTerminal`), which is strictly better — it
  survives a different browser. Nothing to take.
- *Mouse-report filtering.* Withdrawn under review in C9 and it stays withdrawn: the dashboard
  console is a real terminal emulator and `server.js` states the raw-passthrough contract
  explicitly. Note hermes agrees — they do **not** filter mouse reports; they add
  `macOptionClickForcesSelection` so the *user* can bypass tracking. That is the right shape if we
  ever want it.
- *Their `\x1b[RESIZE:cols;rows]` in-band resize escape.* We have a typed control action; an in-band
  escape in a byte-transparent channel is a hazard we do not need.

---

## The system as a whole — responsibility audit (2026-07-31)

Everything above is a list of defects. This section asks the different question the operator posed:
**does the shape of this service make sense, and is each responsibility owned in exactly one place?**
Measured, not asserted.

### What is cleanly owned today — protect these, they were expensive

- **Status is DERIVED on the server and only RENDERED on the client.** Checked: the dashboard has no
  status derivation. `resolveStatus` (`status.js:49`) maps a server-supplied string to a display
  kind and nothing else — no clock, no liveness reasoning, no fallback logic that could disagree with
  `derive()`. This is the single most valuable boundary in the system and it currently holds. It got
  that way through a deliberate rewrite (proof-based status, 2026-07); it would be cheap to lose by
  accident and should be pinned by a test rather than by discipline.
- **The bridge observes, the server decides.** PTY liveness is observed locally by the bridge because
  it is the only process that can; every *conclusion* drawn from it is the server's. WS-9's design
  rule ("label locally-observed vs server-derived and never blend them") is that boundary written
  down.
- **Pure logic is being pulled out of untestable scripts.** `doctor-predicates.js`,
  `register-identity.js`, `cli-resume.mjs`, `sessions-list.mjs`, `terminal-input.mjs`. Each extraction
  has caught a real bug the moment it became testable. The pattern works; it is under-applied, not
  wrong.

### H1. The system's core vocabulary has no single home — it is hand-copied across the language boundary

The 6-state agent status is THE shared contract of this service: agents read it to decide whether to
send, the dashboard filters on it, the reconciler acts on it. It is declared authoritatively in
`status_engine.py:19`:

```python
VALID_STATUSES = ("working", "online", "available", "blocked", "offline", "stopped")
```

and then re-declared, by hand, in JavaScript — **four more times**, none of them bound to the source:

| Copy | Contents |
|---|---|
| `app.js:1644` `SESSION_FILTER_KINDS` | the same six, retyped |
| `app.js:1645` `SESSION_LIVE_KINDS` | the "live" subset — four |
| `chat.js:112` `const live = new Set([…])` | the SAME four, declared independently |
| `status.js` `STATUS_KINDS` | 21 keys: the six agent states + session/terminal states + RUN states |

**Nothing verifies any of them.** No `.test.mjs` references `SESSION_FILTER_KINDS` or
`SESSION_LIVE_KINDS`; the only files mentioning `status_engine` are Python tests. And the vocabulary
is not served by the API, so there is not even a runtime path by which the client could learn it.

**The failure is silent by construction.** `resolveStatus` ends
`STATUS_KINDS[raw] || STATUS_KINDS.unknown` — an unrecognised status does not throw, it renders as
"unknown" in a muted grey chip. Add a seventh state server-side and the dashboard keeps working,
filters it into nothing, and shows those agents as unknown. That is drift with a graceful face, which
is the hardest kind to notice.

This is N12 one tier up: **one rule, many copies, no binding** — except here the copies span two
languages, which is why no amount of carving `api_v2.py` would surface it.

- [ ] Serve the vocabulary (states + which are "live") from the API, or generate the JS constant from
      the Python source. Either way there is ONE writer.
- [ ] Until then, at minimum: a test that the JS copies equal the Python tuple. Cheap, and it converts
      a silent drift into a red suite.
- [ ] Collapse `chat.js:112` into `SESSION_LIVE_KINDS`. Two independent declarations of "which
      statuses are live" is the same defect as N12's two sweeps, with the same fix.
- [ ] Split `STATUS_KINDS`' three vocabularies (agent / session / run) or document that it is
      deliberately a presentation-only union. One table serving three domains means a status name
      that exists in two of them can only have one appearance.

### H2. There are THREE god-files, one per tier — the plan names one

| File | Lines | Share of its tier |
|---|---|---|
| `service/routers/api_v2.py` | 22,903 | **98.5% of all router code** (health.py 174 + containers.py 168 are the rest), 103 routes |
| `mcp/stdio/server.js` | 6,193 | the bridge's entry point and its largest module |
| `service/new_dashboard/app.js` | 4,965 | 58% of dashboard JS |

`service/routers/` is a package with effectively one file in it. WS-1 treats `api_v2.py` as *the*
problem; the measurement says it is the worst instance of a repo-wide pattern.

**This does NOT mean "carve all three"** — that would be exactly the over-reach the scope rule
forbids, and `server.js` and `app.js` have not produced the defect run `api_v2.py` has. What it means
is narrower and cheaper:

- [ ] WS-1's duplicate-rule guard is written to be **tier-agnostic** from the start. N12's predicate
      spans `db.py` and `api_v2.py`; H1's spans Python and JavaScript. A guard that can only see one
      language would have missed the more dangerous of the two.
- [ ] New behaviour in any tier goes in a new module, never into the god-file. The dashboard already
      has this convention (`service/new_dashboard/*.mjs` with adjacent tests) and CLAUDE.md states it;
      the service tier has no equivalent statement. Say it for `service/` too.

### H3. "Can this environment act?" has four implementations and exactly one name

| Question | Where | Answer |
|---|---|---|
| Can it host a managed SPAWN? | spawn picker, `api_v2.py` | `online` only |
| Is the agent REACHABLE at all? | 11 gates in `api_v2.py` | `{online, degraded}` |
| Can its bridge execute a queued CONTROL? | `db.py`, 3 uses | `_environment_actionable_sql()` ← the only one with a name |
| Is the fleet healthy? | `doctor-predicates.js` | `online` only, deliberately matched to the spawn picker |

These are **not** all the same question, so four answers is not automatically wrong — a degraded
bridge can accept a message but must not host a new spawn. The defect is that only one of the four
says which question it answers. The other three are inline conditions a reader must reverse-engineer,
and N7 happened precisely because two of them silently disagreed about `degraded`.

The v0.1.1 fix named one. Naming the rest is small, mechanical, and it is what makes the next
disagreement visible instead of latent.

- [ ] Give the other three predicates names that state the QUESTION (`env_can_host_spawn`,
      `env_is_reachable`, `env_is_fleet_healthy`), and make every call site use them.
- [ ] One test per predicate over the whole `ENV_KNOWN_STATES` domain — the shape that caught N7's
      second divergence.
- [ ] Then a doc table like the one above in DECISIONS.md, so "why are these different?" is answered
      once instead of re-litigated per reviewer.

### The through-line

Every finding in this document — N1's phantom vocabulary, N7's disagreeing sweeps, N12's eight
copies, H1's five copies, H3's four unnamed predicates — is the same structural fact:
**this system's shared concepts are expressed as inline literals rather than as named, tested,
single-writer definitions.** The god-files are why that is hard to see; they are not the cause.

That reframes WS-1. Carving `api_v2.py` makes the copies *visible*; it does not remove them. The
higher-value half of WS-1 was always the guard, and this audit says the guard should be aimed at
**named shared definitions**, not at one error string. That is a smaller, more testable, and more
durable target than the carve — and it is the one that would have caught the most defects in this
document.

---

## Workstreams

Numbered for reference, **not** in ship order — see "Release sequencing" below for that. WS-1 is the
backbone everything structural lands on top of, but it is explicitly not the gate for the release.

### WS-1 — Carve up `api_v2.py` (ledger E1) · **backbone**

22,889 lines in one module. The argument is in "What v0.2 is for": this file is why a rule can exist
twice and why a reviewer cannot see it.

Carve **by seam, lowest-risk first, one seam per commit, suite green and a deploy between each.**
Every seam commit must be a **pure move** — no behaviour edits in the same commit, so the diff is
reviewable as "did this move, unchanged?" and a bisect lands on a move or a change, never both.

Suggested seam order (verify line counts before starting; they will have drifted):
1. `terminal_snapshot` / console + terminal endpoints
2. terminal-control lifecycle + the reconcile sweeps ← **this seam is where the duplicate rule lives**
3. dispatch / claim / delivery gates
4. status + `derive()` + the live-state cache
5. messages / channels / files
6. agents / sessions / environments

Gates:
- [ ] **ONE rule = ONE module.** Add a guard test that the string `terminal is not active` (and the
      `action='stop'` exemption predicate) appears in exactly one module. The v0.1 defect was
      invisible to review; make it visible to CI. Generalise to a small registry of
      "rules that must be unique" as WS-1 finds more of them.
- [ ] `_LIVE_STATE_CACHE` and `_LIVE_SCREENS` live in exactly one module and are imported, never
      duplicated. A second copy silently breaks the single-worker invariant.
- [ ] No file over ~1,500 lines when the seam is done. That is not the 500-line guidance, but it is
      a 15× improvement and reachable without inventing new abstractions.
- [ ] Deploy after each seam. Do not stack six unverified structural commits.

### WS-2 — Console input: find the submit mechanism, stop the dead noise (C8, C9, N3)

v0.1 fixed the **honesty** half — the endpoint and the MCP tool now say QUEUED, not sent. This is
the **mechanism** half. It cost an operator's manager agent ~15 minutes of critical-path idle across
five stalls, and the console tail was the only arbiter throughout while status read `online`.

**Root-cause hypothesis, now with upstream corroboration.** `api_v2.py:14110` builds
`body = text + ("\r" if enter else "")` — **one control, one write, text and CR together**. That is
exactly the pattern hermes deliberately avoids (three separate call sites, 100 ms apart, with the
reason in the comment). And we already have the discipline in this repo:
`terminal-runtime.js:582 _sendAnswer` sends keystrokes as a **spaced sequence** with
`autoAnswerKeyDelayMs = 150` precisely so a menu move re-renders before the next key. The console
path skips a discipline the repo already proved it needs.

That yields a chain which explains the *whole* report, including the part that previously looked
contradictory:

1. one write containing text **and** CR → Ink's tokenizer coalesces it into a **paste**, not
   keypresses;
2. a paste with a trailing newline in claude's composer **inserts a newline** instead of submitting;
3. the draft is now **multi-line**, so every subsequent bare `\r` inserts another newline;
4. each retry looks identical and does nothing — "worked twice, then silently stopped submitting".

This is why the ledger's warning ("do NOT reach for write-splitting first — three bare-CR writes
also failed, and a lone CR cannot lose to state batching") is **still correct and not a
contradiction**: splitting prevents *entering* the poisoned state; it cannot *recover* from a draft
already made multi-line. Both halves are needed.

- [ ] **DISCRIMINATING TEST — REPLACED; the previous one was not discriminating.** I had specified
      "send a bare `\r` to a stuck draft and read the tail by eye". A reviewer pointed out that proves
      almost nothing: on an **already-poisoned** draft a newline appearing confirms only that the
      composer is currently multi-line, which is the *symptom*, and says nothing about whether the
      combined write caused it. It cannot distinguish the hypothesis from its negation.

      The test must run on a **FRESH** draft and compare the two writes directly:
      1. Fresh composer. Send `"hello" + "\r"` as **one** control (today's behaviour). Record the tail.
      2. Fresh composer. Send `"hello"`, wait ~150 ms, send `"\r"` as a **separate** control. Record.
      3. **Prove submission end-to-end**, not from the tail: the message must appear as a real turn —
         a reply, a run transition, a transcript entry. A tail is the thing that has already fooled us
         once (`consoleBytesBefore` was removed for exactly this), and a repainting spinner means the
         bytes changing proves nothing.

      Only if (2) submits and (1) does not is the paste-coalescing chain confirmed. If **both**
      submit, the chain is wrong and the poisoning has another cause — in which case do not ship the
      split as a fix, because it would be a coincidence dressed as a cure.
- [ ] Then: send text and CR as **separate** writes with a delay, via a shared helper rather than a
      fourth open-coded copy (see WS-4.4). Reuse `_sendAnswer`'s discipline; do not re-derive it.
- [ ] Then: a **recovery** lever for an already-poisoned draft, since splitting does not help there.
      Needs the runtime's real submit sequence — claude-TUI behaviour this repo cannot verify from
      outside, so this step is gated on the test above, not on reasoning.
- [ ] C9: **do not enqueue a control for a terminal that is already not active.** 158 rows of
      guaranteed-dead work buried C8's two real writes when reading the table. Pure win, no contract
      change, and it makes the table readable during the next incident.
- [ ] N3: make both branches of `_resolve_live_console_terminal` agree about `'stopping'`.

### WS-3 — Verification that works on the primary dev host (ledger B1)

`bridge-running` and `agent-identity` both **skip** on Windows ("process inspection is Linux-only").
The primary dev host is Windows. So after `install.sh`, nothing verifies that a live wrapper is
executing post-install code — the exact failure mode CLAUDE.md opens by warning about ("every deploy
path in this repo fails silently").

Do it platform-independently rather than porting `/proc` scraping:

- [ ] Bridge reports the build stamp it loaded at boot on `POST /agents/{id}/heartbeat`.
- [ ] Server persists it on `bridge_instances` / `environments`.
- [ ] `aify-doctor` gains `bridge-code-current`: names any live bridge whose **reported** stamp
      differs from the **installed** stamp, with "relaunch these wrappers" as the fix.
- [ ] Retire the Linux-only `bridge-running` once this lands. Two checks that skip are worse than
      one that runs.
- [ ] Extract + test the build-stamp comparison and the usage classification (the B2 leftovers) —
      same treatment as the env predicates, lower stakes.

### WS-4 — Port the four verified hermes deltas

- [ ] **4.1 — DEFERRED on the scope rule.** Extracting the console reconnect predicates into tested
      modules is testability work on behaviour that already works and has produced no defect. It
      earns its place only when something needs to CHANGE there (WS-2's submit path is the likely
      trigger). Do it then, with a real reason, not as a tidy-up. Behaviour-identical extraction
      only, if ever. Keep
      any callback-facing predicate **arity-1** — the v0.1 arity trap is the case study, and a test
      should assert the arity, not just the behaviour.
- [ ] **4.2** Gate WebGL on host width (`>= 768`), with the DOM renderer below it. One-line change,
      fixes "huge text" on narrow viewports and in DevTools device mode.
- [ ] **4.3** Close-cause surfacing for the real PTY console: log `code` + `reason` on every close,
      and give a terminated console an explicit "start a new session" affordance instead of a dead
      pane. Adopt distinct codes for *process exited* vs *superseded by a newer attach*.
- [ ] **4.4** One shared "type text then submit" helper for every path that drives a TUI composer,
      replacing the open-coded copies. WS-2 is its first consumer.

### WS-5 — Status-vocabulary truth (N1, N2)

The measurement is done; the work is deciding what the writers *should* produce. See N1/N2. Do this
**after** WS-1 seam 6 (agents/sessions), when the writers are in one readable module.

### WS-6 — Dispatch / delivery backlog

Existing tracked items, each already traced. Bring them in behind WS-1:
- [ ] **#240** `/messages/send` is not retriable — a reply dropped on a transient socket error is
      lost. Wants an idempotency key. Highest user-visible value in this group.
- [ ] **#238** Channel posts to members whose environment is offline are stored but never replayed
      when the environment recovers, so they never wake.
- [ ] **#239** Promised-work-without-wake backstop, in the shape of the reply-reminder heuristic.
- [ ] **C6** — one implementation task, not a decision. The decision is already made below
      (`delivered` is NOT terminal); a reviewer caught that this bullet still asked for it, which
      would have sent an implementer to re-litigate a settled call. Work: add the reconcile rule that
      advances a run carrying `finished_at` to a terminal status, keep `delivered` meaning "handed to
      the target, outcome unknown", and backfill the existing rows in the same commit with the count
      verified before and after.
- [ ] **C3** `_is_turn_busy_fresh` is production-dead: delete it and fold its assertion into
      `_turn_busy_state` coverage, or make the reminder loop use it. Do not leave a shared "busy"
      definition that only tests call.
- [ ] **C4** Doc-only: record in DECISIONS.md that pre-migration queued runs read as non-steerable
      and drain at turn-end (not "within 30 min"), so it is not re-diagnosed as a bug.
- [ ] **C5** When any WebSocket client gains prompt submission, give it an explicit `onRequest` —
      today a fall-through auto-**denies** codex approvals with `-32601`. Test it.

### WS-7 — Observability (ledger D2, D3)

The v0.1 strand was invisible: the dashboard showed an idle agent whose queued work could never be
claimed. The ceiling now bounds it; the symptom still has no display.

- [ ] Attention-strip row for "has queued work but is not claiming", with the reason
      (`turn_busy latched` / `no steer capability` / `bridge offline`).
- [ ] And the reverse: a run `delivered` with `requireReply` and no reply past N minutes.
- [ ] Expose the last N `_run_dispatch_reconcile_once` counter dicts (`GET /reconcile/last` or an
      ops panel). They are the fastest fleet diagnosis available and nothing displays them.
- [ ] D1: make the chat `all` identity discoverable ("All traffic (read-only)").

### WS-9 — `aify-comms` becomes a live status screen instead of a rolling log · **the highest-value new surface**

**Operator ask, 2026-07-26, verbatim intent:** typing `aify-comms` should give "some TUI dashboard
kind of thing" showing state and connections — not a scrolling log.

**What `aify-comms` is today.** The wrapper (`install.sh:2555`) prints three lines and then
`exec node …/server.js --environment-bridge`. So it is a **long-lived foreground supervisor** the
operator leaves running in a terminal: it owns this host's PTYs, polls
`/terminals/controls/claim` (800 ms, long-polled), claims dispatches, heartbeats the environment, and
emits ~78 `console.*` call sites as it goes. It already holds, in-process and first-hand:
`MACHINE_ID`, `BRIDGE_INSTANCE_ID`, `BRIDGE_VERSION`, `BRIDGE_STARTED_AT`, and
`computeBridgeBuildTag()` (`server.js:177-224`).

**This is a better home for the TUI than a separate tool, and it supersedes an earlier draft of this
item.** I had specified a standalone `aify-top` that would re-derive fleet state over HTTP. That was
aimed at the wrong thing. The bridge *already is* the process that knows which terminals are alive,
which claims are in flight, and whether its last heartbeat succeeded — locally, without asking
anyone. A second process polling the API to guess at what this one observes directly is strictly
worse: more code, more latency, and a second opinion about liveness, which is the exact shape of
doctor's false green. **`aify-top` is dropped.** Two surfaces, not three: `aify-comms` = live, this
host; `aify-doctor` = one-shot proof, whole system.

**The actual defect in a rolling log**, and therefore the design principle: **errors scroll away.**
Steady-state heartbeat and claim chatter pushes the one line that mattered off the screen, so the
operator is left scrolling for something that already left the buffer. So: **state is sticky, noise
is bounded.**

Panes (this host's bridge, not the fleet — the dashboard covers the fleet):
1. **Header** — server URL, machine/env id, bridge instance id, **build tag**, uptime, and the age of
   the last successful heartbeat. Connection to the service as a state with an age, never a bare dot.
2. **Terminals this bridge owns** — agent, runtime, session mode, terminal id, PID, and whether the
   PTY is actually alive. Observed locally, so it is the one place this fact is authoritative.
3. **Work** — claims in flight, last claim outcome, queued/failed counts.
4. **Errors — sticky.** The last N failures with count and age, which do **not** scroll. This is the
   pane that fixes the actual complaint.
5. **Recent activity** — the log, bounded to a pane instead of the whole scrollback.

Non-negotiables, each earned by a specific past failure:
- [ ] **TTY → status screen; non-TTY → today's line log, byte-identical.** Piped, `nohup`'d,
      service-managed and CI invocations must not change at all. `--log` forces line mode in a TTY.
      This is the compatibility contract and it is what makes the change safe to ship.
- [ ] **PREREQUISITE, not an existing property: the bridge must write a logfile.** I stated that the
      full stream "still" goes to one, citing `LOG_ROOT` (`install.sh:713`). A reviewer checked:
      `~/.local/state/aify-comms/` exists but holds only `codex-aify` app-server logs, and `server.js`
      has no `createWriteStream` / `appendFile` / LOG_ROOT write at all. **The environment bridge does
      not write a logfile today** — its diagnostics live only in the terminal scrollback. So bounding
      what is on screen would genuinely *destroy* evidence, which makes this a blocking prerequisite:
      land the logfile first, verify it captures a full session, and only then bound the screen.
      Rendering selectively must never mean recording selectively.
- [ ] **Label locally-observed vs server-derived facts, and never blend them.** PTY liveness is ours;
      an agent's status is the server's `derive()`. Showing them in one row without saying which is
      which is how a surface starts lying. Server-derived values carry an age.
- [ ] **Read-only.** No kill/restart keybindings. On 2026-07-02 a sweep of a live ops UI fired real
      Stop controls and killed three workers plus the operating session — in a screen you stare at
      *during* an incident, a stray keypress is that same hazard with a shorter fuse. Actions stay in
      the dashboard and the MCP tools, where they are single-target and confirmed.
- [ ] **NO `--json --once`. WITHDRAWN — it would have killed the operator's live bridge.** I specified
      it for agent/human symmetry without tracing what a second invocation does. A reviewer did:
      `aify-comms` *is* the environment bridge, so a second process **registers the environment,
      becomes the current bridge, and the server supersedes the incumbent** — which then exits by
      design (`server.js:2643-2649`: on a `server:superseded-bridge` control whose `bridgeId` is not
      its own, the older bridge logs and exits). A diagnostic invocation would therefore terminate the
      running bridge and every PTY it owns. That is the "a tool used during an incident must not make
      it worse" rule violated by the very flag added to satisfy a symmetry principle.

      Rejected the alternative too: owner-process IPC to read the incumbent's state is new machinery
      in the highest-consequence process on the host, for a need that is **already served** —
      `aify-doctor --json`, `comms_agents` and `comms_envs` all answer this without owning a bridge.
      So the screen is a *rendering of the process that is already running*, and there is no second
      invocation. Symmetry is satisfied by doctor, not by making the supervisor dual-mode.
- [ ] Add a test that no code path can start a bridge registration in a render-only mode. The failure
      mode here is silent and destructive, so it needs a pin, not a comment.
- [ ] **Shared predicates.** Anything about environment liveness comes from `doctor-predicates.js`,
      not a fresh copy.

**PONYTAIL CUT — ship the cheap half first.** The named defect is that *errors scroll away*, not that
there is no full-screen UI. Phase 1 is therefore a bounded sticky-error block plus one periodic
status line in the existing log stream — a fraction of the work, and it fixes the actual complaint.
Only build the full-screen panes if Phase 1 proves insufficient in use. Do not start with the panes.

Implementation: **plain ANSI, no TUI framework.** Adding a React runtime to a supervisor that must
keep working when things are broken is the wrong trade, and hermes maintaining an entire *fork* of Ink
is that dependency's cost made visible. Install stays symmetric and unchanged in shape — same
dotfolder copy, no new native module, nothing new for `bridge-terminal` to fail on, and no new
command to learn: the thing the operator already types just gets useful.

Note this makes **WS-3 nearly free**: the build tag the header displays is the same value the
heartbeat needs to send. Same field, two consumers.

### WS-10 — WITHDRAWN: make containers observable → *should containers exist here at all?*

I proposed making the container layer diagnosable. Then I checked: the manager is **never
constructed** — the endpoint 503s and the startup log never mentions it, because `main.py:382` gates
construction on a `containers.definitions` block the live config does not have. 740 lines of Docker orchestration — GPU allocation, idle reaper, health loop, request proxy — serve
nothing on this deployment, and N4/N5/N6 are findings about code no one runs.

Making an unused subsystem observable is the exact scope creep this plan criticises elsewhere. The
honest question underneath it: **sub-container orchestration is not inter-agent communication.**
Managing LocalAI/vLLM containers is infrastructure orchestration living in the same process as the
comms hub — a second product sharing a database and an event loop.

- [ ] **Operator decision: is `service/containers/` wanted-but-dormant, or dead weight?** It predates
      this work, so the call is not mine.
- [ ] If dead weight: delete it, its router, its models and N4–N6 with it. That is worth more than
      any observability it could gain, and it shrinks the surface the single-worker constraint has to
      hold.
- [ ] If wanted: it needs a defined container before ANY of N4–N6 matters. Revisit then, not now.

### WS-8 — The compaction stall dwell (ledger A1b) · **last, and gated**

The one item whose cure is more dangerous than its disease. A wrong dwell presses Enter on a
progressively-painted menu, selects "Resume from summary", and **silently loses context the operator
explicitly chose to preserve** — unrecoverable and fleet-wide. The stall is visible and recoverable.
The previous attempt was reverted for exactly this.

- [ ] Ship the **mitigation** first and separately: a pure predicate ("compaction dialog owns the
      screen AND is unanswerable") surfaced as a status reason. Injects **zero** keystrokes and turns
      a silent wedge into something the operator can see. Today such an agent reads idle.
- [ ] Only then the dwell, in `terminal-runtime.js` (the stateful caller), **wall-clock, not
      poll-count** — a stalled dialog emits no further output, so an output-counter never advances
      past 1. It must re-read the current tail and re-confirm ownership before acting.
- [ ] The matcher keeps declining. It is a pure function of one frame and must stay safe on one frame.

---

## Release sequencing — REVISED

The first draft made WS-1 the gate for everything. That was wrong, and re-reading it against what
the project is *for* is what changed it: WS-1 is the only workstream with **no user-visible payoff**,
and putting it first means the fleet keeps running with a console lever that silently does not work
and a deploy path nothing verifies. Meanwhile the *actual value* of WS-1 — the guard that a rule is
not implemented twice — needs no carve at all. It is a grep test over the file as it stands today.

**That is the ponytail cut: take the guard now, do the carve as background work.**

### What v0.1.1 is — and the line that divides it from v0.2

**v0.1.1 = "same product, now correct and safe."** Someone on v0.1 can take it without relearning
anything. Shipped 2026-07-29..31, deployed and tagged:

- **N7** — a queued `stop` in a DEGRADED environment was cancelled, so the PTY survived and Start
  spawned a second worker. The sweeps now share ONE definition of "this environment's bridge can
  act" across four call sites, and heartbeat statuses AGE (raw `status='online'` never did).
- **The dashboard wheel handler** bypassed the serialized input queue *and* fired on hover, injecting
  synthetic arrows into whichever agent's live PTY the pointer was over. Input-integrity, not polish.
- **`codex-aify` identity recovery** on a bare `--resume` — the last wrapper without it. No
  `AIFY_AGENT_ID` means status latches with nothing alive able to clear it.
- **`comms_register` warns an identity-less resident** — registration succeeded while the session was
  structurally unable to report turns. Warning only; no behaviour change.
- **No hardcoded host identity** — the service URL is asked for at install, derived at runtime, and
  one machine's IP/hostname/account name left the public repo.
- Rendering and interaction defects: the subject echo that read as duplicated messages, the composer
  losing focus to the Options disclosure, replies not clearing the peer's unread badge, and the
  "Continue in CLI" box that had no stylesheet rules at all and rendered nothing when there was no
  session to resume.

**TWO BEHAVIOUR CHANGES rode along, and an upgrader must know about them.** They are shape changes,
not defect fixes, and by the rule above they belong in a minor release. They shipped in the patch
anyway because each was actively misleading an operator *that day*, and leaving a known-misleading
control in place to satisfy a versioning rule is the wrong trade:

- **The "Queue if busy" checkbox is gone**, replaced by the Queue half of a split Send button. The
  checkbox was sticky and hidden inside a collapsed disclosure, so one tick queued every later Enter
  invisibly. A per-send choice must not have a persistent mode.
- **The Sessions list collapses to one entry per agent.** Live rows always show (two live rows stay
  visible — that is a duplicate-worker leak, not clutter); otherwise the newest non-live row, with a
  toggle that reveals the rest.

Anything of that shape from here on waits for v0.2.

### v0.2 — new surfaces and structural work

The dividing line: **v0.2 changes what the product IS**, not whether it is correct. Nothing below is
a bugfix an operator is currently waiting on.

- **WS-3 Windows-real deploy verification** and **WS-1's duplicate-rule guard** — FIRST. Both are
  load-bearing and invisible, both are small, and both are where the recurring defect classes come
  from. Ahead of anything reported this week.
- **WS-9 `aify-comms` sticky errors + status line** (Phase 1 only) — the rolling log loses errors.
- **N8** — the cold-start refusal that names a cause it never checked. Contained, and it cost a
  teammate time today.
- **N12 + H1 raise WS-1's guard from idea to spec** — a named pair of live duplicate sweeps, an
  eight-copy predicate, and a vocabulary hand-copied across the Python/JS boundary five times with
  nothing binding the copies. Build the guard against NAMED SHARED DEFINITIONS, tier-agnostic, not
  against one error string.
- **H1's binding test** — assert the JS status vocabulary equals `status_engine.VALID_STATUSES`.
  Small, and it converts a silent drift (an unknown status renders as a grey "unknown" chip and
  filters into nothing) into a red suite.
- **H3's naming pass** — three unnamed environment predicates get names that state their question.
  Mechanical, and it is what makes the NEXT disagreement visible rather than latent, which is exactly
  how N7 was found.

**N9/N10/N11 are v0.1.2 candidates, not v0.2.** By this file's own dividing line they are
"same product, now correct": three surfaces asserting something they never checked, two of them in
code v0.1.1 shipped. None changes what the product is. Recommendation is a patch release; the
operator's call, and the reason to take it soon is that N10 is showing a wrong command *right now*.
- **WS-2 the console submit MECHANISM** — the honesty half shipped in v0.1 and the real input-path
  defects behind it shipped in v0.1.1, but the discriminating test and the actual submit fix are
  still open.
- **WS-1** — the duplicate-rule guard test now, the `api_v2.py` carve as v0.2.x background work.
- **WS-4.2/4.3**, then WS-4.4, WS-5, WS-6, WS-7 on the seams they belong to. WS-4.1 is deferred
  until something needs to change there — see its entry; do not schedule it as a tidy-up.

**v0.2.x — incremental, one seam per release.** WS-1's carve, deployed seam by seam. WS-4.4,
WS-5, WS-6, WS-7 land on top of the seams they belong to. Nothing here is held back by the release.

**v0.3 — WS-8.** The compaction dwell wants its own release with its own review round. It is the one
change whose failure mode is worse than the bug it fixes.

---

## Decisions

### DECIDED (operator delegated these on 2026-07-26: "do as you think is right")

**A2 — managed codex auto-approve → make it an explicit named setting; behaviour unchanged.**

The auto-approve itself is not the problem and should not change: a managed worker has no operator to
answer a prompt, so without it the run deadlocks. That is a requirement, not a preference. The problem
is **asymmetry**, and symmetry across harnesses is a stated goal of this project. Today hermes'
permissive posture is an explicit, greppable env flag (`HERMES_YOLO_MODE=1`) while codex's is an
emergent property of three RPC handlers (`codex-session.js:202`,
`controllers/codex-legacy-controller.js:218`, `:228`). An operator reading the config cannot discover
that codex is more permissive than hermes. That is the defect — not the permission.

So: one named setting, resolved the same way for every runtime, documented in DECISIONS.md as a stated
property, which makes "managed codex is YOLO" answerable by reading instead of by tracing three call
sites.

**CORRECTED after review — my first version of this decision was self-contradictory.** I wrote
"default ON for managed and **OFF for resident** … zero behaviour change" two paragraphs after
establishing that the handler **is** wired into the resident path. Both cannot be true: resident codex
auto-approves *today*, so defaulting resident to OFF is a real, unflagged behaviour change to tested
behaviour — the "a default change is not a migration" slip, in the same entry that documents it.

The decision, restated honestly:
- [ ] The setting's default **reproduces current behaviour exactly**: auto-approve where it happens
      today, including resident. That is what makes it a naming change with no fleet risk.
- [ ] Pin current behaviour per path with tests **first** — including that resident does get the
      handler — so the baseline is recorded rather than assumed. The ledger's old claim that resident
      never did was false; do not now assume the opposite without a test either.
- [ ] **Whether resident codex SHOULD auto-approve is a separate operator decision**, made
      deliberately and shipped as its own change with its own review. It is a security posture
      question, it belongs with the standing "security fail-open defaults" decision from the
      2026-06-28 audit, and it must not ride along inside a refactor labelled behaviour-neutral.

**C6 — `delivered` is NOT terminal. Every finished run must reach a terminal status.**

Two reasons, and the second is the decisive one:

1. *It is nearly free.* Every consumer already gates on `result_message_id`, not on `status`, so
   advancing the status changes no delivery behaviour whatsoever. This is pure bookkeeping repair —
   the lowest-risk shape a fix can have in this repo.
2. *The alternative is bad for agents, which is who this is for.* "Document `delivered` + `finished_at`
   as terminal and fix the readers" pushes a **two-field rule** onto every future reader, and the
   primary readers are agents. An agent asking "is this run still open?" should get its answer from one
   field in one read. A rule requiring it to correlate `status` with `finished_at` will be got wrong —
   by agents, by the dashboard, and by me.

So `delivered` keeps its honest meaning — *handed to the target, outcome unknown* — and a reconcile
rule advances a run that has `finished_at` to a terminal status. The 13 existing rows get a one-shot
backfill in the same commit, with the count verified before and after (not assumed: reading back an
artefact I produced is slip 5).

### RECOMMENDED — the one call with a real cost attached

**Do the carve, but ship it as v0.2.x rather than gating v0.2.0 on it.** Rationale in
"Release sequencing" above. The reason to still do it: at 22,889 lines this file is where the defect
classes come from, not merely where they live, and three of this round's own findings (N1, N2, N3)
are set-mismatch and branch-disagreement bugs that only exist because nobody can hold the module in
context. Deferring the carve indefinitely means accepting that class permanently. Deferring it by one
release, with the guard test landing now, costs almost nothing.

---

## Deliberately NOT doing (the scope rule, applied)

- **Container observability (WS-10)** — zero containers defined; the real question is deletion.
- **Console-predicate extraction (WS-4.1)** — testability churn on working code with no defect.
- **A full-screen TUI as the first step of WS-9** — the complaint is lost errors; fix that first.
- **Mouse-report filtering (C9)** — withdrawn under review; breaks the raw-PTY contract.
- **Session-history retention (R1)** — deleted rather than repaired in v0.1. If it ever comes back it
  needs the correlated `EXISTS` re-verify the superseded-spawn reaper already uses, **plus** a real
  migration: a `DEFAULT_SETTINGS` change does not un-set a persisted value.
- **A Start gate keyed on live terminals** — superseded. The root cause was fixed by re-targeting a
  queued stop at the environment's current bridge; a gate would hide the duplicate rather than prevent
  it, and a too-strict Start gate is what made the whole `ef-` team unstartable in v0.1.

Also out of scope, for reasons specific to each:

- Changing the virtual-terminal `'stopped'` write. Correct by design: `vterm_%` rows are excluded
  from every liveness query, and `'stopping'` would leave a phantom console for 900s.
- `_current_agent_session_row`'s `ORDER BY`. Deliberately untouched in v0.1; its comment says the
  ordering is kept stable so a relaunch does not pick a different session.

---

## Release gates

v0.2 ships when all of these hold:

- [ ] All three suites green **on Windows**: Python, bridge, dashboard. The dashboard suite was red
      and unrun for the whole v0.1 series and I reported "green" from the other two — that is the E2
      gap, and it is a gate now, not a note.
- [ ] `aify-doctor --strict` exits 0 on the deploying host, **including** the new
      `bridge-code-current` check (WS-3). A green doctor that skips the check is the false green this
      tool exists to prevent.
- [ ] The duplicate-rule guard test exists and passes (WS-1's guard — in v0.2.0; the carve is v0.2.x).
- [ ] **(rewritten — the previous gate required the WITHDRAWN `--json --once` flag.)** The
      `aify-comms` screen and the dashboard agree on every environment's online-ness and on every
      terminal this bridge owns, checked against a live fleet **by reading both surfaces**, not by
      invoking a second bridge. Two surfaces disagreeing is the defect WS-9 exists to avoid creating;
      a gate that mandates a flag the same document forbids is how a withdrawn item gets rebuilt.
- [ ] `aify-comms` piped to a file produces byte-identical output to today's rolling log, and the
      logfile is complete in both render modes. The compatibility contract, tested, not assumed.
- [ ] `service/containers/` has either been deleted or has at least one defined container. Shipping
      a dormant 740-line subsystem unexamined is what the scope rule exists to stop.
- [ ] The C8 discriminating test has been **run against a live stuck draft** and its result recorded
      here — not reasoned about.
- [ ] An external review round on the full diff, with a REVISE→fix cycle, before the tag. Five of
      five v0.1 rounds found something real; assume this one will too.
